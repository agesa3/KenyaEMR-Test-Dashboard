import React from 'react';
import logo from '../images/kenyaemr.png'

const 
 = (props) => {
  return (
    <img
      alt="Logo"
      src={logo}
      {...props}
    />
  );
};

export default Logo;
